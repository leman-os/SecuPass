# 🔐 SecuPass — Генератор запоминающихся паролей

[![Docker](https://img.shields.io/badge/Docker-Ready-blue?logo=docker)](https://www.docker.com/)
[![Docker Compose](https://img.shields.io/badge/Docker_Compose-Supported-blue?logo=docker)](https://docs.docker.com/compose/)
[![React](https://img.shields.io/badge/Frontend-React-61DAFB?logo=react)](https://reactjs.org/)
[![Node.js](https://img.shields.io/badge/Backend-Node.js-339933?logo=nodedotjs)](https://nodejs.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
![Version](https://img.shields.io/badge/Version-1.0.0-orange)

**SecuPass** — это современное веб-приложение для генерации надёжных, но легко запоминающихся паролей на русском языке.  
Полноценное решение с раздельным фронтендом и бэкендом, готовое к развёртыванию в Docker.

> **English**: SecuPass is a password generation service that creates strong yet memorable passwords using Russian words.

---

## ✨ Особенности

- 🧠 **Запоминающиеся пароли** — основаны на трёх русских словах
- 🔢 **Автоматическая сложность** — цифры, заглавные буквы, спецсимволы
- ⚙️ **Гибкие настройки** — управление сложностью и словарями через админ-панель
- 🐳 **Docker-поддержка** — полная контейнеризация с Docker Compose
- 🏗️ **Современный стек** — React + Node.js + Nginx
- 🛡️ **Безопасность** — настраиваемые учётные данные администратора

---

## 🧩 Как это работает?

Пароль формируется по схеме:

```
[3 случайных числа] + [первые 3 буквы трёх слов] + [спецсимвол]
```

**Пример:**  
Слова: **Кошка**, **Стол**, **Река** → Пароль: `742КосСтоРек!`

**Преимущество:**  
Запомнив всего три слова, вы легко восстановите пароль, даже если забудете его точное написание.

---

## 📁 Структура проекта

```
├── backend/                    # Бэкенд на Node.js
│   ├── database.js           # Конфигурация базы данных
│   ├── init-db.js           # Инициализация БД
│   ├── package.json         # Зависимости бэкенда
│   └── server.js           # Основной сервер
│
├── frontend/                  # Фронтенд на React
│   ├── index.html           # Главный HTML
│   ├── nginx.conf          # Nginx для фронтенда
│   ├── package.json        # Зависимости фронтенда
│   ├── src/
│   │   ├── App.css         # Стили приложения
│   │   ├── App.jsx         # Главный компонент
│   │   ├── index.css       # Глобальные стили
│   │   └── main.jsx        # Точка входа
│   └── vite.config.js      # Конфигурация Vite
│
├── docker-compose.yml        # Docker Compose конфигурация
├── Dockerfile               # Монолитный Dockerfile
├── Dockerfile.backend       # Докерфайл для бэкенда
├── Dockerfile.frontend      # Докерфайл для фронтенда
├── nginx.conf              # Основной Nginx конфиг
├── supervisord.conf        # Конфигурация Supervisor
└── README.md               # Документация
```

---

## 🖼️ Скриншоты

### Главная страница генератора
![Генератор паролей](https://github.com/user-attachments/assets/e30eceaa-d3c7-4bca-9af8-6ec523349534)

### Админ-панель (управление сложностью)
![Настройки сложности](https://github.com/user-attachments/assets/ead53ab0-3d83-43b5-85df-3b139039bd)

### Управление словарём слов
![Управление словарём](https://github.com/user-attachments/assets/56388c2d-2b08-4a20-b748-5aeb5046afd0)

---

## 🚀 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

Самый простой способ запустить все компоненты:

```bash
# Клонируйте репозиторий (если ещё не сделали)
git clone <repository-url>
cd secupass

# Запустите все сервисы
docker-compose up -d

# Приложение будет доступно по адресу:
# http://localhost:8080
```

### Вариант 2: Ручная сборка Docker образов

```bash
# 1. Сборка фронтенда
docker build -f Dockerfile.frontend -t secupass-frontend:latest .

# 2. Сборка бэкенда
docker build -f Dockerfile.backend -t secupass-backend:latest .

# 3. Сборка монолитного образа (альтернативный вариант)
docker build -t secupass-monolithic:latest .
```

### Вариант 3: Разработка без Docker

#### Запуск фронтенда:
```bash
cd frontend
npm install
npm run dev
# Откройте http://localhost:5173
```

#### Запуск бэкенда:
```bash
cd backend
npm install
npm start
# Сервер запустится на http://localhost:3000
```

---

## ⚙️ Настройка

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Бэкенд
ADMIN_USERNAME=admin
ADMIN_PASSWORD=НовыйПароль123!
DATABASE_URL=sqlite://./passwords.db
PORT=3000

# Фронтенд
VITE_API_URL=http://localhost:3000
```

### Конфигурация через Docker Compose

Все переменные можно задать в `docker-compose.yml`:

```yaml
environment:
  - ADMIN_USERNAME=admin
  - ADMIN_PASSWORD=ВашПароль123!
  - DATABASE_URL=sqlite:///data/passwords.db
```

### Пользовательские настройки Nginx

Файлы конфигурации Nginx:
- `nginx.conf` — основной конфиг
- `frontend/nginx.conf` — конфиг для фронтенда

---

## 📊 API Endpoints

Бэкенд предоставляет следующие API:

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/password/generate` | Генерация нового пароля |
| `POST` | `/api/password/validate` | Проверка сложности пароля |
| `GET` | `/api/words` | Получение списка слов |
| `POST` | `/api/words` | Добавление нового слова |
| `DELETE` | `/api/words/:id` | Удаление слова |
| `GET` | `/api/settings` | Получение настроек сложности |
| `POST` | `/api/settings` | Обновление настроек |

---

## 🔧 Администрирование

### Доступ к админ-панели
1. Перейдите по адресу `http://localhost:8080/admin`
2. Введите учётные данные (по умолчанию: admin/НовыйПароль123!)

### Возможности админ-панели:
- ✅ Изменение длины чисел в пароле
- ✅ Выбор доступных спецсимволов
- ✅ Управление словарём (добавление/удаление слов)
- ✅ Настройка количества слов в пароле
- ✅ Сброс к стандартным настройкам
- ✅ Просмотр статистики генерации

---

## 🐳 Docker Compose сервисы

Приложение состоит из трёх сервисов:

```yaml
services:
  frontend:    # React приложение на Nginx
  backend:     # Node.js API сервер
  database:    # SQLite база данных (опционально)
```

### Порты по умолчанию:
- **8080** — фронтенд и API (через Nginx прокси)
- **3000** — бэкенд напрямую (только для разработки)

---

## 🔐 Безопасность

### Рекомендации по развёртыванию в production:

1. **Измените пароль администратора**:
   ```bash
   docker-compose run backend node change-password.js новый-сложный-пароль
   ```

2. **Включите HTTPS**:
   - Добавьте SSL сертификаты в `nginx.conf`
   - Настройте редирект с HTTP на HTTPS

3. **Ограничьте доступ к админ-панели**:
   - Настройте IP-фильтрацию в Nginx
   - Используйте базовую HTTP аутентификацию

---

## 🐛 Устранение неполадок

### Проблема: "Cannot connect to backend"
```bash
# Проверьте статус контейнеров
docker-compose ps

# Просмотрите логи бэкенда
docker-compose logs backend

# Перезапустите сервисы
docker-compose restart backend
```

### Проблема: "Database not initialized"
```bash
# Инициализируйте базу данных вручную
docker-compose exec backend node init-db.js
```

### Проблема: "Frontend not loading"
```bash
# Проверьте Nginx конфигурацию
docker-compose exec frontend nginx -t

# Перезапустите фронтенд
docker-compose restart frontend
```

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле [LICENSE](LICENSE).

---

## 🤝 Вклад в проект

Мы приветствуем ваши предложения и pull requests!

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📧 Поддержка

По вопросам и предложениям:
- Создавайте [Issue](https://github.com/ваш-репозиторий/issues) на GitHub
- Или свяжитесь с разработчиками через репозиторий

---

## 🔄 История изменений

### v1.0.0
- Первый стабильный релиз
- Полная Docker контейнеризация
- Разделение фронтенда и бэкенда
- Админ-панель для управления

